pipeline{
        agent any
        stages{
            stage('Build'){
                steps{
                    sh 'exit
                    echo ${pwd}
                    git pull origin setup-front-end
                    su - jenkins'
                }
             stage('Success'){
                echo "Pipeline successful"
             }
            }
        }
}
